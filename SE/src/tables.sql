create table product(
	id VARCHAR(255) PRIMARY KEY,
	name VARCHAR(255),
	category VARCHAR(255)
);

create table customer(
	id VARCHAR(255) PRIMARY KEY,
	name VARCHAR(255),
	gender VARCHAR(255)
);
